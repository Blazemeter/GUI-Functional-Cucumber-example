package config;

public class BzmConfig {
    //TODO: set the api key, token, project id and location id (replace {locationId} with real id):
    public static final String API_KEY = "";
    public static final String API_TOKEN = "";
    public static final String PROJECT_ID = "";
    public static final String LOCATION_ID = "harbor-{locationId}";
}
